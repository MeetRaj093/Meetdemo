<?php
// use Brain\Monkey\Functions;
// class WCVendors_Pro_Order_Controller_Test extends WCVendorsTestCase {
	// We can't test die. Leave the test commented for now.
	// public function test_shipping_label() {
		// $wcv_vendors = Mockery::mock( 'alias:WCV_Vendors' )->makePartial();
		// $wcv_vendors->shouldReceive( 'get_vendor_shop_name' )
			  // ->once()
			  // ->andReturn('Test Store' );
		// $order = Mockery::mock('WC_Order');
		// $order->shouldReceive('get_items')
			// ->withNoArgs()
			// ->andReturn([
				// ['product_id' => 1],
				// ['product_id' => 2],
			// ]);
		// Functions\expect('wc_get_order')->once()->andReturn($order);
		// Functions\expect('get_user_meta')
			// ->with(Mockery::type('int'), '_wcv_store_country', true)
			// ->andReturn('US');
		// Functions\expect('get_user_meta')
			// ->with(Mockery::type('int'), Mockery::type('string'), true)
			// ->andReturn('store_address');
		// $wcv_queries = Mockery::mock( 'alias:WCV_Queries' )->makePartial();
		// $wcv_queries->shouldReceive('get_products_for_order')->with(Mockery::type('int'))->andReturn([1,2]);
		// Functions\expect('wp_get_attachment_image')
			// ->with(Mockery::type('int'))
			// ->andReturn('img_html');
		// Functions\expect('wc_get_template')
			// ->with(
				// Mockery::type('string'),
				// Mockery::type('array'),
				// Mockery::type('string'),
				// Mockery::type('string')
			// )
			// ->andReturn(true);
		// WCVendors_Pro_Order_Controller::shipping_label(1, 1);
	// }
// }
